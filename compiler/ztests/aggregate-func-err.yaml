# Test that an error is returned if an aggregate function
# is called in expression context.
spq: |
  put count := count()

error: |
  call to aggregate function in non-aggregate context at line 1, column 14:
  put count := count()
               ~~~~~~~
