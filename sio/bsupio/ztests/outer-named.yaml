script: super - | super -s -|super - | super -s -

inputs:
  - name: stdin
    data: &stdin |
      type typ=int64
      0::typ
      type typ2={x:int64}
      {x:0}::typ2

outputs:
  - name: stdout
    data: *stdin
