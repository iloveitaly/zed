spq: defuse(this)

input-flags: '-i fjson'
output-flags: '-f json -pretty=0'

input: &input |
  {"b":true,"a":[{"x":1}]}
  {"a":[]}

output: *input

---

spq: defuse(this)

input-flags: '-i fjson'
output-flags: '-f json -pretty=0'

input: &input |
  {"a":[]}
  {"b":true,"a":[{"x":1}]}

output: *input

---

spq: defuse(this)

input-flags: '-i fjson'
output-flags: '-f json -pretty=0'

input: &input |
  ["foo"]
  []
  []
  ["bar"]

output: *input
