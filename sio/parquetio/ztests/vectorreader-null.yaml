script: |
  super -f parquet -o test.parquet in.sup
  # from operator uses NewVectorReader
  super -s -c 'from test.parquet'

inputs:
  - name: in.sup
    data: &in |
      {s:null}

outputs:
  - name: stdout
    data: *in
