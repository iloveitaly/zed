script: |
  super -f jsup - | super -s -i jsup -

inputs:
  - name: stdin
    data: &input |
      1
      2
      "hello"
      [3,4]
      {a:1}
      map{}
      true
      false
      1::(int64|string)
      "foo"::(int64|string)
      <int64>
      set[1,2,3]
      null

outputs:
  - name: stdout
    data: *input
