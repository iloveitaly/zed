# Test string first
spq: min(this), max(this), sum(this), avg(this)

input: |
  "foo"
  1

output: |
  {min:error("mixture of string and numeric values"),max:error("mixture of string and numeric values"),sum:error("mixture of string and numeric values"),avg:1.}

---

# Test number first
spq: min(this), max(this), sum(this), avg(this)

input: |
  1
  "foo"

output: |
  {min:error("mixture of string and numeric values"),max:error("mixture of string and numeric values"),sum:error("mixture of string and numeric values"),avg:1.}
