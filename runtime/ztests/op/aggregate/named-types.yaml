spq: count() by host | sort host

vector: true

input: |
  type ipaddr=ip
  {host:127.0.0.1::ipaddr}
  {host:127.0.0.2}

output: |
  type ipaddr=ip
  {host:127.0.0.1::ipaddr,count:1}
  {host:127.0.0.2,count:1}
