spq: where this

input: |
  1
  null
  true
  true::(bool|null)
  fusion(true::(bool|null),<bool>)
  error("divide by zero")
  error("missing")

output: |
  true
  true::(bool|null)
  true

---

# Tests where with op in front so it doesn't get pushed down.
spq: 'values this | where this'

input: |
  1
  null
  true
  true::(bool|null)
  error("divide by zero")
  error("missing")

output: |
  true
  true::(bool|null)
