spq: put eq := (a == b), ne := (a != b), lt := (a < b), lte := (a <= b), gt := (a > b), gte := (a >= b)

input: |
  {a:1,b:null}

output: |
  {a:1,b:null,eq:null,ne:null,lt:null,lte:null,gt:null,gte:null}
