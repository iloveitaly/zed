# Tests bug where use of under in sequential runtime would cause an infinite loop
spq: unnest this | collect(this)

input: '[null,1,"a"]'

# XXX This output is incorrect and should be in the same order as input.
output: |
  [1,"a",null]
