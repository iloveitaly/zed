spq: fuse | defuse(this)

input: &input |
  [{x:1},{y:1}]

output: *input

---

spq: fuse | defuse(this)

input: &input |
  "foo"::(int64|string)
  "foo"::=named
  "foo"::(named2=int64|string)

output: *input
