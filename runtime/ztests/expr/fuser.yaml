spq: fuse | defuse(this)

input: &input |
  [{x:1},{y:1}]

output: *input

---

spq: fuse | defuse(this)

input: &input |
  "foo"::(int64|string)
  type named1=string
  "foo"::named1
  type named2=int64|string
  "foo"::named2

output: *input
