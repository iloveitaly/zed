spq: |
  values
    exists(select * from (values ("foo"), ("bar"))),
    "===",
    exists(values 1),
    "===",
    exists(values "foo", "bar" | where this == "baz")

output: |
  true
  "==="
  true
  "==="
  false
