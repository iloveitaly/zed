spq: fuse | defuse(this)

vector: true

input: &input |
  [1,2]
  ["foo"]
  [null]

output: *input

---

spq: fuse | defuse(this)

input: &input |
  {x:1}
  {x:2,y?:3}

output: *input

---

spq: fuse | defuse(this)

input: &input |
  {a:{b:1}}
  {a:{b:1,c:2}::=n1}::=n2

output: *input

---

spq: fuse | defuse(this)

input: &input |
  1::=p1
  {a:{b:1::=r1}::=r2}::=r3
  [[1::=a1]::=a2]::=a3
  |[|[1::=s1]|::=s2]|::=s3
  |{1::=m1:2::=m2}|::=m3
  "a"::(en1=enum(a,b))
  1::=n1::(n2=n1|(n3=string))
  error(1::=er1)::=er2

output: *input

---

spq: defuse(this)

input: |
  {a:fusion(1::(int64|string),<int64>)}

output: |
  {a:1}
