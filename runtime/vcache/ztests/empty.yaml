script: |
  super -f csup -o test.csup in.sup
  super dev vector copy -s test.csup

inputs:
  - name: in.sup
    data: &input |
      {}
      []
      map{}
      set[]

outputs:
  - name: stdout
    data: *input
