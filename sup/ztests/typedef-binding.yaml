spq: |
  type port=uint16
  type rec={p:port}
  is(r,<rec>)

input: |
  type port=uint16
  type rec={p:port}
  {r:{p:80::port}::rec}
  {s:"hello"}

output: |
  type port=uint16
  type rec={p:port}
  {r:{p:80}::rec}
